import React from 'react';
import { customeFilterProps } from '../types';

const CustomeFilter = ({ title }: customeFilterProps) => {
    return <div>CustomeFilter</div>;
};

export default CustomeFilter;
